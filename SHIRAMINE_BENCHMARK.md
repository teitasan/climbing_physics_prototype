# 白峰マップ：ベンチマークメモ

基準日：2026-09-10（JST）  
対象：Mesh Terrain Lab から保存した `meshterrain-godot-20260910004119.zip` から取り込んだ白峰・東峰マップ。

## 地形データ

| 項目 | 記録 |
| --- | --- |
| ワールドサイズ | 4,096 m × 4,096 m |
| セクション | 128 m、32 × 32 = 1,024パッチ |
| 地形メッシュ | 585,976頂点 / 1,855,839インデックス / 618,613三角形（約62万ポリゴン） |
| 編集内容 | 19 modifiers、レイヤー「白峰・登山道」 |
| LOD解像度（元エクスポート設定） | 88 / 44 / 22 / 11 / 6 |
| 元ツールの目標FPS | 30 fps |

三角形数は `assets/environment/meshterrain-shiramine/assets/world.glb` の1,024個の `terrain_patch` を直接数えた値。GLBには水面用の追加メッシュも含まれるため、上の値は地形部分だけの値として扱う。

## ファイルとテクスチャ

| 項目 | 記録 |
| --- | --- |
| 地形GLB | 44,700,340 bytes（約42.6 MiB） |
| テクスチャ | 7枚、PNG合計15,565,838 bytes（約14.8 MiB） |
| テクスチャ解像度 | 1,024² RGBA8 × 6枚、256² RGBA8 × 1枚 |
| テクスチャ展開後の目安 | 約24.25 MiB（RGBA8生データ） |
| GLB + テクスチャ | 60,266,178 bytes（約57.5 MiB） |
| 取り込みフォルダ全体 | 66,231,483 bytes（約63.2 MiB、source JSON・ルート・shader等を含む） |
| Mesh Terrain Labの保存ZIP | 76,201,049 bytes（約72.7 MiB） |

テクスチャはエクスポートされたTGAを、画質を変えない可逆PNGに変換してプロジェクトへ同梱した。`albedo` 2枚、`normal` 2枚、`arm` 2枚、地質ディテール1枚で構成される。

## ブラウザ実測

測定対象は `http://localhost:8125/shiramine.html`。M1 Pro（14コアGPU、メモリ16GB）、ブラウザ表示領域726 × 781 CSS px、devicePixelRatio 2、レンダラーのピクセル上限約1.9M pxで確認した。

登山口の通常視点では画面表示のHUDが約369千三角形、100〜120 fps。全体表示や山頂付近では約540〜553千三角形。`shiramine.html?test=1` の実キャラクター歩行検証は、436地点・15,241フレームを停止・落下なしで走破した。

継ぎ目対策後は、登攀専用の非表示コリジョンプロキシを追加している。地形上面約545,264三角形と境界ブリッジ約39,580三角形で構成し、描画三角形数には加算されないが、衝突用ジオメトリとBVHのCPUメモリには影響する。

このブラウザ版はエクスポート済みGLBを一括読み込みしている。Mesh Terrain Lab本体のセクションストリーミング／LODとは実装が異なるため、同じ地形でもメモリ使用量を比較するときは「ファイル容量」と「表示中三角形」を分けて記録する。

元エクスポート設定には `maxGpuBytes=256 MiB`、`maxCpuCompiledBytes=384 MiB`、`maxEditableMeshBytes=192 MiB` が記録されている。これはMesh Terrain Lab側の予算であり、ブラウザ版の実使用量そのものではない。

## 次回比較用

地形を作り直したときは、同じ条件で「worldSize / patch数 / 地形三角形 / GLB容量 / テクスチャ枚数と最大解像度 / ZIP容量 / 登山口FPS / 全体表示FPS」を追記する。特に、三角形を増やすより先にテクスチャ解像度と同時表示範囲を確認すると、重さの原因を切り分けやすい。
